<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= job.jobName %> - Job Details</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
  <style>
    body {
      background-color: #f8f9fa;
    }
    .card {
      border-radius: 12px;
    }
    h2, h4 {
      color: #007bff;
    }
    img {
      max-height: 200px;
    }
    .btn-success {
      width: 100%;
    }
    @media (max-width: 576px) {
      .card-body {
        padding: 1rem;
      }
    }
  </style>
</head>
<body>

  <!-- Navbar -->
  <nav class="navbar navbar-expand-lg navbar-dark bg-primary">
    <div class="container-fluid">
      <a class="navbar-brand" href="/">Job Bridge</a>
      <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav">
        <span class="navbar-toggler-icon"></span>
      </button>
      <div class="collapse navbar-collapse" id="navbarNav">
        <ul class="navbar-nav ms-auto">
          <li class="nav-item"><a href="/" class="nav-link">Home</a></li>
          <li class="nav-item"><a href="/find" class="nav-link">Find Jobs</a></li>
        </ul>
      </div>
    </div>
  </nav>

  <div class="container my-4">
    <div class="card shadow-lg">
      <div class="card-body">
        <h2 class="card-title text-center mb-3"><%= job.jobName %></h2>
        
        <% if (job.companyImage) { %>
          <div class="text-center mb-3">
            <img src="/uploads/<%= job.companyImage %>" class="img-fluid rounded" alt="Company Image">
          </div>
        <% } %>

        <p><strong>Location:</strong> <%= job.jobLocation %></p>
        <p><strong>Skills Required:</strong> <%= job.jobSkills %></p>
        <p><strong>Package Offered:</strong> <%= job.packagePerYear %> LPA</p>
        <p><strong>Contact Details:</strong> <%= job.contactDetails %></p>
        <p><strong>Description:</strong> <%= job.jobDescription %></p>
        <p><strong>Posted by:</strong> <%= job.uploader ? job.uploader.name : 'N/A' %></p>
        <hr>

        <h4 class="mt-4">Apply for this Job</h4>

        <% if (user) { %>
          <form action="/apply/<%= job._id %>" method="POST" class="mt-3" enctype="multipart/form-data">
            <div class="mb-3">
              <label for="applicantName" class="form-label">Full Name</label>
              <input type="text" name="name" id="applicantName" class="form-control" required value="<%= user.name %>">
            </div>

            <div class="mb-3">
              <label for="applicantEmail" class="form-label">Email</label>
              <input type="email" name="email" id="applicantEmail" class="form-control" required value="<%= user.email %>">
            </div>

            <div class="mb-3">
              <label for="applicantPhone" class="form-label">Phone Number</label>
              <input type="text" name="phone" id="applicantPhone" class="form-control" required value="<%= user.phoneNumber %>">
            </div>

            <div class="mb-3">
              <label for="coverLetter" class="form-label">Cover Letter / Message</label>
              <textarea name="message" id="coverLetter" rows="4" class="form-control" placeholder="Why should you be hired?" required></textarea>
            </div>

            <div class="mb-3">
              <label for="resume" class="form-label">Attach Resume (PDF or DOC/DOCX)</label>
              <input type="file" name="resume" id="resume" class="form-control" accept=".pdf,.doc,.docx" required>
              <small class="text-muted">Max size: 5MB</small>
            </div>

            <button type="submit" class="btn btn-success">Submit Application</button>
          </form>
        <% } else { %>
          <p class="text-danger mt-3">Please <a href="/login">login</a> to apply for this job.</p>
        <% } %>
      </div>
    </div>
  </div>

  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
